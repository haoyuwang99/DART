# ---- DART: keep only src/ (code). Everything below is data, envs, or third-party. ----

# Python envs / caches
.venv311/
.venv/
__pycache__/
*.pyc
*.egg-info/
.ipynb_checkpoints/

# Data & results (large; regenerated by src/run_*.py). Force-add a specific
# small result file with:  git add -f rdeval_records.jsonl
*.jsonl
*.npz
*.npy
log/
asb_data/

# Cloned third-party benchmark repos (clone separately; not ours to commit)
asb_repo/
injecagent_repo/
agentdojo/

# Scratch / build artifacts
*.pptx
*.aux
*.blg
*.fdb_latexmk
*.fls
*.out
*.synctex.gz

# OS
.DS_Store

# Old pre-modularization DART scripts (superseded by src/dart/ package)
src/appendix/

# ---- CAS / dxscore / robustness benchmark: a SEPARATE project, NOT DART ----
CAS/
*dxscore*
dxscore_benchdx_3000_bundle/
fireworks_quickstart.py
judge_robust.py
robust_judged*
